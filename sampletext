As part of ongoing remediation efforts, the team is currently performing a restart of the UIM JVMs to determine if it clears the persistent open cursor buildup and improves query response times.

This step follows previous attempts, including the FedLine Provisioning Engine JVM recycle and ongoing investigation into certificate-related exceptions.