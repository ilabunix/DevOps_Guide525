Update:
The fix has been deployed to production. The database script to remove the impacted link reference was executed, and JVMs have been restarted. The team is currently performing validation tests to confirm the resolution.

Business Impact:
Impact remains low — only two requests have been affected so far.