That’s correct — if the application is deployed in a single region, the certificate only needs to be uploaded in that region. The document serves as a general step guide covering both primary and secondary region scenarios, as most apps are currently configured for dual-region setup in GovCloud. It’s expected that the person performing the renewal understands the deployment scope and applies the steps accordingly for their specific app configuration.