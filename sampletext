Final Update:
The fix has been deployed to production. The database script to remove the impacted link reference was executed, and JVMs were restarted. Post-deployment validation has confirmed the fix, with APIs functioning properly and no failures observed. Additional checks, including subscriber enrollment report verification, have shown no further issues. Normal operations have resumed.

Business Impact:
Impact remained low — only two requests were affected during the incident window.
