We’ve updated the static landing page in the CFS 0.5 SAC QA environment (with help from DMT) to display the new CFS2 UAT SAC URLs. This ensures that if users arrive via the legacy URLs, they are clearly guided to the new locations.