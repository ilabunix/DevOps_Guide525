<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Clara – Your CloudOps Assistant</title>
  <style>
    :root {
      --bg-color: #f7f9fb;
      --text-color: #222;
      --chat-bg: #fff;
      --bubble-bg: #eef6fc;
      --accent-color: #007bff;
    }

    body.dark {
      --bg-color: #121212;
      --text-color: #eee;
      --chat-bg: #1f1f1f;
      --bubble-bg: #2a2a2a;
      --accent-color: #90caf9;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: var(--bg-color);
      color: var(--text-color);
      margin: 0;
      padding: 1rem;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .container {
      width: 100%;
      max-width: 800px;
      background: var(--chat-bg);
      border-radius: 10px;
      box-shadow: 0 4px 14px rgba(0,0,0,0.1);
      padding: 1.5rem;
      display: flex;
      flex-direction: column;
    }

    h1 {
      margin-top: 0;
      font-size: 1.8rem;
      text-align: center;
    }

    .tagline {
      font-size: 0.95rem;
      color: var(--text-color);
      text-align: center;
      margin-bottom: 1rem;
      opacity: 0.7;
    }

    .chat-box {
      max-height: 400px;
      overflow-y: auto;
      padding: 0.5rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      border-top: 1px solid #ccc;
      border-bottom: 1px solid #ccc;
      margin-bottom: 1rem;
    }

    .message {
      background: var(--bubble-bg);
      padding: 1rem;
      border-radius: 8px;
      white-space: pre-wrap;
      line-height: 1.5;
    }

    .user-msg {
      align-self: flex-end;
      background: #d0ebff;
      color: #003366;
    }

    .clara-msg {
      align-self: flex-start;
      border-left: 4px solid var(--accent-color);
    }

    .query-area {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    textarea {
      width: 100%;
      height: 80px;
      padding: 0.75rem;
      font-size: 1rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      resize: vertical;
    }

    .actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    button {
      background: var(--accent-color);
      color: white;
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
    }

    .dark-toggle {
      font-size: 0.9rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🤖 Ask Clara – Your CloudOps Assistant</h1>
    <div class="tagline">From chaos to clarity — with Clara.</div>

    <div class="chat-box" id="chatBox">
      <div class="message clara-msg">💬 Clara is ready to help you!</div>
    </div>

    <div class="query-area">
      <textarea id="query" placeholder="Enter your question here..."></textarea>
      <div class="actions">
        <button onclick="askBot()">Ask</button>
        <label class="dark-toggle">
          <input type="checkbox" id="darkModeToggle"> Dark Mode
        </label>
      </div>
    </div>
  </div>

  <script>
    const queryInput = document.getElementById("query");
    const chatBox = document.getElementById("chatBox");
    const toggle = document.getElementById("darkModeToggle");

    toggle.addEventListener("change", () => {
      document.body.classList.toggle("dark", toggle.checked);
    });

    async function askBot() {
      const query = queryInput.value.trim();
      if (!query) return;

      // Add user question
      const userMsg = document.createElement("div");
      userMsg.className = "message user-msg";
      userMsg.textContent = query;
      chatBox.appendChild(userMsg);

      // Show thinking...
      const thinkingMsg = document.createElement("div");
      thinkingMsg.className = "message clara-msg";
      thinkingMsg.textContent = "🧠 Clara is thinking...";
      chatBox.appendChild(thinkingMsg);
      chatBox.scrollTop = chatBox.scrollHeight;

      try {
        const res = await fetch("https://3vdty0ekf8.execute-api.us-west-2.amazonaws.com/prod/chatbot", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ query })
        });

        const data = await res.json();
        const answer = Array.isArray(data.answer)
          ? data.answer.map(item => item.text || item.content || "").join("\n\n")
          : data.answer || "🤖 No answer generated.";

        // Replace thinking with real response
        thinkingMsg.textContent = `💡 Clara says:\n\n${answer}`;
        chatBox.scrollTop = chatBox.scrollHeight;

      } catch (error) {
        thinkingMsg.textContent = `❌ Error: ${error.message}`;
      }

      queryInput.value = "";
    }

    queryInput.addEventListener("keypress", function (e) {
      if (e.key === "Enter" && !e.shiftKey) {
        e.preventDefault();
        askBot();
      }
    });
  </script>
</body>
</html>